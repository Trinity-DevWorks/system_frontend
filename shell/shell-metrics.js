/**
 * Shared chrome dimensions for the app shell.
 *
 * The header, module rail and page panel all align to the same top band, so the
 * height lives here rather than being repeated per component.
 */

export const SHELL_CHROME_HEIGHT_PX = 48;
export const SHELL_RAIL_WIDTH_PX = 56;
export const SHELL_PANEL_WIDTH_PX = 232;
