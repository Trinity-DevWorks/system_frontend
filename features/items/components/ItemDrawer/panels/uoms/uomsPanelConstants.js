export const UOM_DRAFT_ROW_ID = "__uom_draft__";

/** @typedef {{
 *   uom_id?: number;
 *   currency_id?: number;
 *   conversion_factor: number;
 *   barcode: string;
 *   selling_price?: number;
 *   cost_price?: number;
 *   is_base: boolean;
 *   is_default_sale: boolean;
 *   is_default_purchase: boolean;
 * }} UomInlineValues */
